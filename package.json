{
  "name": "financial-companion",
  "private": true,
  "version": "0.1.0",
  "description": "Bangladesh Financial Companion Platform — monorepo root",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "dev:web": "pnpm --filter @fc/web dev",
    "dev:api": "pnpm --filter @fc/api dev"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.32.1"
}
